const HeroSection = () => (
  <section className="hero p-2 w-full mx-auto my-4 bg-blue-950 rounded-xl md:max-w-screen-sm">
    <h3 className="text-center">
      Let&apos;s grow your online presence and user engagement!
    </h3>
  </section>
);

export default HeroSection;
